<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title or "Cat Management" }}</title>
    <link rel="stylesheet" href="/assets/tailwind.css" />
  </head>
  <body class="min-h-screen bg-slate-950 text-slate-100">
    <nav class="bg-slate-900 border-b border-slate-800">
      <div class="mx-auto max-w-6xl px-4 py-4 flex items-center justify-between">
        <a href="/" class="font-bold text-lg">🐾 Cat Management</a>
        <div class="space-x-4 text-sm">
          <a href="/cats" class="hover:text-emerald-300">Cats</a>
          {% if currentUser %}
            <a href="/account" class="hover:text-emerald-300">Account</a>
            {% if currentUser.roleName in ["admin", "superadmin"] %}
              <a href="/admin" class="hover:text-emerald-300">Admin</a>
            {% endif %}
            <form action="/logout" method="post" class="inline">
              <button class="text-red-300 hover:text-red-200">Logout</button>
            </form>
          {% else %}
            <a href="/login" class="hover:text-emerald-300">Login</a>
          {% endif %}
        </div>
      </div>
    </nav>

    <main class="mx-auto max-w-6xl px-4 py-6">
      {% include "partials/flash.njk" %}
      {% block content %}{% endblock %}
    </main>

    <footer class="mx-auto max-w-6xl px-4 py-6 text-sm text-slate-500">
      &copy; {{ ("" | date("Y")) }} Cat Management Lab
    </footer>
  </body>
</html>

